export  const ProductAantal= (params) =>{
    for(var i=0; i<params.length; i++){
            var j = i-1;
            if(params[i].name==params[j].name){

            } else{
                
            }

    }
}